// import localForage from "localforage";
import { StIAccesoryData } from "./st_accesory_data";

/**
 * TODO
 */
export class StModelManager {
    get(id: number): StIAccesoryData {
        throw Error(`TODO: get accesory by ID: ${id}`);
    }
}
